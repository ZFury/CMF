<?php
$title = 'Add new comment';
$this->headTitle($title);
?>
<h1><?php echo $this->escapeHtml($title); ?></h1>
<?php
/**
 * @var Zend/Form/Form $form
 */
$form = $this->form;
//$form->setAttribute('action', $this->url('user/default', array('controller' => 'signup', 'action' => 'index')));
$form->prepare();
?>
<div class="container">
    <div class="row">
        <?php echo $this->form()->openTag($form); ?>
        <?php echo $this->formHidden($form->get('security')); ?>

        <?php $this->formRow($form->get('comment')); ?>

        <div class="form-group">
            <label for="inputComment" class="col-sm-2 control-label"><?= $form->get('comment')->getLabel(); ?></label>

            <div class="col-sm-3">
                <textarea class="form-control" name="<?= $form->get('comment')->getName(); ?>"
                    value="<?= $form->get('comment')->getValue(); ?>"
                    placeholder="<?= $form->get('comment')->getLabel(); ?>"
                    rows="<?= $form->get('comment')->getOption('rows'); ?>"
                    cols="<?= $form->get('comment')->getOption('cols'); ?>"></textarea>
            </div>
        </div>
        <div class="form-group">
            <div class="col-sm-2"></div>
            <div class="col-sm-3">
                <input type="submit" name="submit" id="submitbutton" class="btn btn-lg btn-primary btn-block"
                       value="<?= $form->get('submit')->getValue(); ?>">
            </div>
        </div>
        <?php echo $this->form()->closeTag(); ?>
    </div>
</div>
