<div data-spy="grid">
    <a class="btn btn-primary pull-left"
       href="<?= $this->url('test/default', ['controller' => 'management', 'action' => 'create']) ?>">Create</a>
    <!--    FILTER-->
    <?= $this->filter($this->grid) ?>
    <!--    GRID-->
    <div class="grid">
        <?= $this->table($this->grid, ['class' => ['table', 'table-fixed', 'table-striped']]) ?>
        <!--        PAGINATION-->
        <?= $this->pagination($this->grid, ['class' => ['pagination', 'pull-left']]) ?>
        <!--    LIMIT-->
        <?= $this->limit($this->grid, ['class' => 'pagination pagination-small pull-right']) ?>
    </div>
</div>
<script>
    require(['starter.grid']);
</script>
