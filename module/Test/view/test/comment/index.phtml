<h3 class="page-header">
    Try to comment this entity!
</h3>
<?php echo $this->headLink()->appendStylesheet($this->basePath() . '/module/comment/css/comment.css')?>
<div class="row">
<div>
    <dl class="dl-horizontal">
        <dt>Id</dt>
        <dt>Name</dt>
        <dd><?= $testEntity->getId() ?></dd>
        <dd><?= $testEntity->getName() ?></dd>
    </dl>
</div>

<?php if (null !== $addCommentForm) : ?>
<div class="col-sm-6">
    <?= $addCommentForm ?>
</div>
<?php endif; ?>
</div>

<div data-spy="grid"  class="row" data-url="<?= $this->url('test/default', ['controller' => 'comment', 'action' => 'index'])?>">
    <ul class="grid col-md-6">
        <?php  if ($comments && is_array($comments) && count($comments)>0) {
            echo $this->comment($comments);
        }; ?>
    </ul>
</div>

<script>
    require(['dashboard', 'comment']);
</script>